const users=[]

//join user to chat
function userJoin(){
//adding username,roll,id in users array
}

//get the current user
function getCurrentUser(userroom){
//matching the id in the users array to get the current room users
}
module.exports = {getCurrentUser}